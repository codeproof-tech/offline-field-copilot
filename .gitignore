# dependencies
node_modules/

# agent / local tooling (keep internal)
.gemini/
.agents/

# secrets / environment
.env
.env.*

# native build outputs (regenerated by `expo prebuild` / gradle — do NOT commit)
android/
ios/
.expo/
.gradle/

# logs / temp
*.log
npm-debug.*
yarn-error.*

# signing keys / keystores — NEVER commit
*.keystore
*.jks
*.pem
*.key
*.p12

# qvac build output
qvac/
